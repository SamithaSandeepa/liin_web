// TypeScript interfaces for About page data structures

export interface TeamMember {
  img: string;
  name: string;
  role: string;
  bio: string;
}

export interface Value {
  icon: string;
  title: string;
  description: string;
}

export interface Milestone {
  year: string;
  title: string;
  description: string;
}
